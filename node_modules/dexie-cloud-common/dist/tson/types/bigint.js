export const bigintTypeDef = {
    bigint: {
        replace: (realVal) => {
            return { $t: "bigint", v: "" + realVal };
        },
        revive: (obj) => BigInt(obj.v),
    },
};
export default bigintTypeDef;
